# RH front, stone 2: the nonlinear system works — and it validates against hard literature

**Date:** 2026-08-02. **Script:** `codex/rh_mobius3_system.py`. Follows stone 1
(`RH_T3_CORRECT_CARRIER_2026-08-02.md`), which closed the affine route.

## Results

**Q1 — the affine obstruction is gone.** The depth-3 Möbius system
`φ_k(x) = 1/(x+k)`, k ∈ {1,2,3}, transfer operator on H²(D(1,3/2)), is nuclear
(spectrum stable to machine precision, N = 40 vs 80 identical to 8 digits) and
its spectrum is **non-geometric**: successive ratios 3.16, 2.82, 2.73, 2.69,
2.67 — drifting, not constant. Nonlinearity delivers genuine spectral content
where the affine base-3 shift provably could not.

**Q2 — external validation against independent hard numerics.** The operator's
dimension datum: λ_max(s*) = 1 at

    s* = dim_H(E₃) = 0.705660908029

E₃ = the continued-fraction Cantor set with digits ≤ 3. Jenkinson–Pollicott's
high-precision value for dim_H(E₃) is 0.70566... — **our operator reproduces an
independently computed, famously hard constant.** This is the kind of check the
L² experiments never had: agreement with someone else's number, not our own.

**Q3 — the framework graft is well-posed.** ch20's ℤ₃ cube phases ω^k placed on
the branches give a twisted operator that is equally nuclear (truncation drift
2.8×10⁻¹⁶). So character/representation twists of this operator are
numerically and analytically viable.

## Why this is the real road, stated honestly

Mayer's theorem: for the FULL alphabet k ∈ {1,2,…} this exact operator family
satisfies det(1−L_s)·det(1+L_s) = Z_Selberg(s) for PSL(2,ℤ). Chang–Mayer
extends to congruence subgroups Γ₀(N) via character/representation twists —
precisely the ℤ₃/mod-3 structure the framework insists on, pointing at Γ₀(3).

**And in the Selberg world, Pablo's mechanism is TRUE.** The Selberg zeta's
nontrivial zeros lie on the critical line BECAUSE they are eigenvalues of a
self-adjoint operator (the Laplacian on the modular surface). "Self-adjoint
operator forces the zeros onto the line" — the book's central intuition — is a
theorem there, and the transfer-operator route (this operator class) reaches
it. The open jump is Selberg → Riemann: no known self-adjoint home for ζ
itself. That jump IS the Hilbert–Pólya problem; nothing here closes it, and we
say so.

## Next milestones (each falsifiable / kernel-targetable)

M1. Full-alphabet Mayer operator numerically: verify det(1±L_s) zeros against
    known even/odd Maass eigenvalues for PSL(2,ℤ) (Hejhal's tables).
M2. The ω-twisted full operator vs Γ₀(3)/χ mod 3 spectral data (Chang–Mayer).
M3. Lean target selection: nuclearity of a contraction-system transfer
    operator on H² — assess mathlib's `Polynomial`/`analytic` infrastructure;
    if formalizable, it is the first transfer-operator theory in any prover
    and the rigorous foundation under M1/M2.
