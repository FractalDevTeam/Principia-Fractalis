Issued by Pablo Cohen, 2026-09-07, relayed by Dispatch.

Principia Fractalis — Unified-Theory Proof Directive. Primary objective: organize all work toward a kernel-verified proof of the unified theory claimed by Principia Fractalis.

§1 Governing objective: the unified-theory proof is the controlling objective; every task evaluated by whether it advances, tests, or protects it. ξ campaign, finite-height RH, book, publicity, empirical corroboration are bounded supporting programs only. Canonical doc UNIFIED_THEORY_PROOF_PROGRAM.md (central theorem, dependency graph, open obligations, countermodel risks, next decisive step). Gate: no task enters the queue without stating the obligation it advances, the failure mode it tests, the trust property it protects, or an explicit non-core cap.

§2 Completion theorem first: exact Lean signature of mathematical completion; strongest honest version now + weaker reachable version; no bundled predicates (StructuralLaws/PhysicalAdmissibility-style) hiding content; expose foundational assumptions, constructed universe, equivalence relation for uniqueness, every claimed sector, every claimed-derived constant, correspondence statements, mathematical-vs-empirical split; field-by-field explanation; list premises that restate conclusions. Not accepted unless a reviewer sees the substantive assumptions from #check @ + unfolds.

§3 Backward dependency graph: machine-readable ledger; every load-bearing node exactly one of: definition-or-convention / theorem-derived-from-earlier-foundations / independently-motivated-axiom / empirical-input / conditional-interface / unresolved-conjecture / recorded-negative-or-refuted. Record source file, FQN, deps, #print axioms, closed-term status, rebuilt-from-source status. Human-readable generated from machine-readable. Unknown provenance = blocker.

§4 α-skeleton rigidity = first mathematical gate: reconcile r124 one-parameter family with r128 L1–L5-conditional uniqueness. Per law: unfold fully; earliest independent source; derivable from foundations?; imports target?; remove and survey surviving family; countermodel search; verdict necessary/redundant/derivable/independent/circular. No replacement assumptions to rescue uniqueness without the same audit. No "unique/forced/derived" labels until kernel theorem + premise audit jointly support the exact word. Output: adversarial rigidity report (verdicts, dependency paths, countermodels or bounded failed searches, minimal sufficient subsets, residual parameter space, strongest non-circular uniqueness theorem).

§5 Sector interfaces: per sector (geometry/topology, quantum, gravitation, cosmology, consciousness, others) a separate interface theorem from the common foundational object, distinguishing construction / governing identities / uniqueness / numerical specialization / physical interpretation / empirical correspondence. Shared notation, common fitted parameter, post hoc agreement ≠ unification. Sector matrix rows=claims cols=construction/derivation/uniqueness/correspondence/empirical/trusted-assumptions, green cells linked to theorem + audit. "Derived" only when no premise contains the sector's target in equivalent form.

§6 Two claim ladders, never collapsed: mathematical (definitions→existence→laws→rigidity→sector derivations→unified theorem) and empirical (observable map→preregistered prediction→protocol→data→uncertainty→replication). Kernel verification is not evidence nature instantiates premises; empirical fit is not proof of derivation. Per physical claim record theorem, observable, units, dataset, prediction date, uncertainty model, dof, prospective/retrospective. "Confirmed prediction" only if fixed before data examined and survives the registered rule.

§7 Kernel-trust everywhere: no sorry/admit/hidden axiom/Prop:=True/bypasses; no RC-only acceptance; explicit #print axioms; #check @ for unconditional claims; exact arithmetic verdicts; explicit treatment of native evaluation, opaque computation, external solvers, generated certificates; committed provenance + clean rebuild. Any theorem beyond [propext, Classical.choice, Quot.sound] quarantined until explained and approved. One unexplained axiom blocks the release claim.

§8 Adversarial program, equal priority: remove assumptions singly; construct non-isomorphic models; parameter-family searches; relabeling-detection via definitional equivalence; vacuous-premise and unused-hypothesis detection; cross-sector consistency; alternate structures producing the same constants; formalize recorded-negatives that constrain the theory. UNIFICATION_COUNTERMODEL_LEDGER.md. Failed search ≠ nonexistence unless formally exhaustive.

§9 Auxiliary campaigns bounded: r331c/d permitted for reusable verified infrastructure, the precisely scoped finite-height theorem, reproducible proof engineering — never delaying the rigidity audit or completion-theorem spec; own claim ladder (box→boundary→count→evaluated count→finite-height RH); never promoted as evidence for global RH or unification absent a formal dependency. Every auxiliary gets ceiling, stopping condition, named reusable deliverable; exceeding ceiling auto-pauses and escalates to Pablo.

§10 Reports = proof-state changes, exact order: 1 central theorem status; 2 became proved; 3 became disproved/weaker; 4 blocking obligation; 5 assumption/circularity changes; 6 kernel+rebuild status; 7 decision required from Pablo; 8 next decisive action. One screen; ledgers attached not reproduced. If nothing changed: "No change to the unified-theory proof state."

§11 Execution order: 1 freeze/verify r331b artifact without broadening claims; 2 draft completion theorem + reachable weaker form; 3 backward dependency ledger; 4 rigidity audit as first decisive gate; 5 revise signature/roadmap from verdict; 6 sector-interface matrix; 7 auxiliaries within ceilings; 8 empirical only via preregistered falsifiable tests with explicit observable maps.

§12 Prohibited substitutions: theorem/line counts; compilation without axiom inspection; conditional uniqueness whose premises encode the target; one selected model amid a parameter family; numerical coincidence without a derived observable map; retrospective fit as prediction; finite-height RH; global RH by itself; shared terminology across sectors; book narrative unsupported by the formal chain.

§13 Success: Lean verifies a closed, auditable theorem showing independently stated foundations determine — up to the explicitly defined equivalence — the common structure and claimed sector interfaces, all load-bearing assumptions exposed, every canonical theorem passing kernel and clean-rebuild gates. Physical validation is a second, separate objective: explicit prospective predictions surviving measurement and independent replication. Until both: exact scope language. The kernel decides derivability; the dependency audit decides substance; observation decides nature.
