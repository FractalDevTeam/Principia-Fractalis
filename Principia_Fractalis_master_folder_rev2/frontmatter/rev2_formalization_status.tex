\chapter*{Formalization Status (Revision 2)}
\addcontentsline{toc}{chapter}{Formalization Status (Revision 2)}

\section*{Current state (2026-05-18 addendum)}

The Lean~4 + Coq formalization has substantially advanced since the
April~2026 baseline documented below. The 2026-05-18 session also
audited and corrected numerous numerical errors across Chapters~03, 07,
08, 09, 10, 11, 20, 21, 23, 24, 25, 26 (see ``Manuscript-correction
log'' below). The current authoritative state:

\begin{itemize}
\item \textbf{Lean 4}: 5736 jobs clean, \textbf{1 verified project axiom}
  (\texttt{alpha\_class\_polylog\_eigenvalue\_conjecture}), 0 sorries.
  The May~2026 Stage~25--35 unification reduced the earlier
  6--8 axiom state to a single axiom that is the formal encoding of
  Chapter~21's polylog Conjecture + branch-selection Heuristic +
  golden-modulation Conjecture. Specific values $\alpha_P = \sqrt{2}$
  and $\alpha_{NP} = \varphi + 1/4$ are derived \emph{theorems} from
  this axiom, not separately axiomatized. The 2026-05-18 session added
  $\sim 100$ axiom-free theorems certifying manuscript-level numerical
  claims and several manuscript-error certificates that drove direct
  manuscript corrections.
\item \textbf{Cross-prover Coq}: 10 modules clean with the same
  1-axiom state; the headline reduction chain (operator algebra,
  spectral-gap consequence, P~$\neq$~NP capstone) is mirrored, and
  the Phase~A Cantor-substrate matrix-entry framework's algebraic
  spectral content is mirrored at the abstract-kernel level.
\item \textbf{Axiom dependency verified} via \texttt{\#print axioms}:
  \texttt{P\_neq\_NP\_via\_spectral\_gap} depends only on Lean's
  standard \texttt{propext}, \texttt{Classical.choice}, \texttt{Quot.sound}
  + the single project axiom;
  \texttt{riemann\_hypothesis\_via\_T3\_sym\_framework} depends on the
  three standard axioms only (\emph{zero project axioms}) as a
  4-hypothesis conditional, the load-bearing fourth hypothesis being
  surjectivity of the spectral bijection onto $\zeta$-zeros (the open
  problem of the RH program).
\item \textbf{Phase~A retirement framework}: 50+ \texttt{PF/Analytic/}
  Lean modules, all zero-project-axiom, comprising two complementary
  spectral attacks: the $L^2([0,1])$ cosine/sine basis framework
  (Mercer rank-2-per-scale decomposition, polylog Hankel identity for
  all $\mathrm{Re}\,s > 0$, Hilbert--Schmidt compactness with
  $\|H_P\|_{HS} \leq a/(a-1)$) and the Cantor-substrate matrix-entry
  framework (IFS contractions, Hutchinson operator,
  \texttt{cantorDiscMeasure}~$n$ discrete approximations, complete
  finite-rank spectral theorem at level~1 with orthonormal eigenbasis,
  IFS-reflection block decomposition at level~2, Banach-contraction
  analytic engine via $(1/3)^n$ Lipschitz shrinkage). The deep
  operator-theoretic derivation of the polylog formula on the physical
  Riemann sheet remains the open mathematical problem.
\item \textbf{Open Problems catalog} at \texttt{OPEN\_PROBLEMS.md}
  authoritatively enumerates the four open problems isolated by the
  framework (three for P~$\neq$~NP axiom retirement; one =
  RH surjectivity = the load-bearing open problem of the RH program).
  Problem~2 (branch selection) was narrowed 2026-05-18: $M_0$-monodromy
  sheet selection is formally ruled out (theorem
  \texttt{manuscript\_target\_unreachable\_via\_M0\_sheet} in
  \texttt{PF/Analytic/PolylogSpectrum.lean}); the open problem is now
  oriented toward non-$M_0$ mechanisms.
\end{itemize}

\section*{Manuscript-correction log (2026-05-18 session)}

A systematic audit of the manuscript's numerical claims against the Lean~4
formal-verification framework found and corrected the following errors,
each anchored to formally-certified Lean theorems in
\texttt{PF\_Lean4\_Code/PF/MillenniumSixReductions.lean}:

\begin{itemize}
\item \textbf{Ch~03 resonance-values table}: Three $\alpha$ values
  ($\alpha_{NS}$, $\alpha_{BSD}$, $\alpha_{Hodge}$) didn't match the
  canonical $\alpha$-Dictionary (frontmatter). Corrected to match.
\item \textbf{Ch~07 cortical-network derivation}: The transition
  $p_{\text{coherence}} = 1 - p_c \approx 1 - 0.05 = 0.95$ uses
  $p_c = 0.05$ but the value derived above was $p_c = 0.0204$. Disclosed
  as arithmetic inconsistency; $1 - 0.0204 = 0.9796$, not $0.95$.
\item \textbf{Ch~08 \& Ch~26 cosmological-constant derivations}: Both
  chapters claimed $\exp[-\text{(large exponent)}] \approx 10^{-120}$
  for derivations whose exponents are actually $\sim 10^{50}$ or
  $\sim 10^{128}$ orders of magnitude too large. Disclosed; the observed
  $\Lambda_{\text{eff}}/\Lambda_0 \approx 10^{-120}$ ratio is empirical
  fact, but the consciousness-suppression mechanism does not numerically
  reproduce it.
\item \textbf{Ch~09 line~115}: $\lambda_0(H_{NP}) = \pi/(10(\varphi+1/4))
  \approx 0.1330$ was claimed; the formula actually evaluates to
  $\approx 0.1682$. Corrected.
\item \textbf{Ch~09 line~195}: $\alpha$ derivation formula gives
  $\approx 0.031$, not $5 \times 10^{-6}$ as claimed. Disclosed.
\item \textbf{Ch~10 Re$_c^{\text{crit}}$}: Both $e^{-1.114} \approx 2.13
  \times 10^{-1}$ (actual $\approx 3.28 \times 10^{-1}$) and
  $(10/\pi)(\pi/10) = 1$ (actual $\neq 2.13198$) were wrong.
  Corrected to $\approx 3.28 \times 10^5$.
\item \textbf{Ch~11 ``Why 4 Dimensions''}: ``$4 + 8.55 \approx 4$''
  asserted, but $4 + 8.55 = 12.55$. Disclosed.
\item \textbf{Ch~20 verification status}: Updated to record that the
  three earlier-edition gaps (Li$_1$ monodromy, sine-identity mismatch,
  $\lambda_{NP}/\lambda_P$ inconsistency) have been addressed via the
  Ch~21 corrections below.
\item \textbf{Ch~21 multiple errors}: (a) ``Sine identity verification''
  double error ($|0.798/0.847| \neq 0.5988 \neq (\sqrt{5}-1)/3$); (b)
  ``Evidence 3 independence'' false (Evidence~3 algebraically equals
  Evidence~2 rescaled by~$1/10$); (c) Spectral Gap Analysis triple
  error ($\pi(\sqrt{5}-1)/(30\sqrt{2}) \neq 0.1330$; $\pi(4-\sqrt{5})/
  (30\sqrt{2}) \neq 0.0891$; $\alpha_{NP}$ typo); (d) Golden Ratio
  Emergence double error (empirical ratio $0.5988 \neq (\sqrt{5}-1)/3$).
  All four corrected. (e) New $M_0$-narrowing remark inserted.
\item \textbf{Ch~23 exercise errors}: (a) Mass-gap exercise used the
  removed-$\pi/10$ formula (Remark~\ref{rem:pi-10-removed-ym} of Ch~23)
  giving $\approx 132.16$ MeV$\cdot$fm rather than $420.43$ MeV; (b)
  Glueball-masses exercise conflated fractal-operator prediction with
  lattice values; (c) String-tension exercise formula dimensionally
  inconsistent. All three corrected.
\item \textbf{Ch~24 BSD $\varphi/e$}: The value $0.59634736\ldots$
  claimed in six occurrences across two manuscript folders is
  numerically incorrect; actual $\varphi/e \approx 0.59524158\ldots$.
  All occurrences corrected.
\item \textbf{Ch~25 Hodge ch$_2$}: $0.95 + 0.118/10$ asserted to equal
  $\approx 0.9612$; actual value $\approx 0.9618$. Corrected.
\end{itemize}

\noindent Each correction is anchored to a Lean theorem that formally
certifies the error and the corrected value. The pattern across these
corrections is consistent: the manuscript contains numerical claims and
``derivations'' that look like internal consistency checks but, when
the arithmetic is actually performed, do not hold. Surfacing these
honestly --- correcting the numbers and reframing failed derivations
as open problems --- strengthens rather than weakens the framework, since
the structural conjectures remain and the open derivation work is now
explicitly catalogued.

\noindent The historical record below (April~2026, V01 reconciliation pass)
remains accurate for that snapshot but is superseded by the May~2026
unification arc and the 2026-05-18 audit corrections. See
\texttt{AXIOM\_AUDIT.md},
\texttt{PRISTINE\_CERTIFICATION.md}, and \texttt{THE\_REAL\_SCIENCE.md}
at the repository root for the current authoritative state.

\hrulefill

This revision accompanies substantial work on the Lean~4 formalization of the book's
central claims. As of 2026-04-26 (GitHub master commit \texttt{76c7699} and later, plus
post-rev-2 verification updates documented below):

\section*{⚠ Verification check pending V01 reconciliation (2026-04-27)}

In a verification pass conducted after the rev~2 frontmatter above was
written, a numerical/symbolic computation applied to the operator and
inner product as transcribed verbatim from Chapter~20 and the Lean
source did not confirm the self-adjointness identity claimed in
Chapter~20 Theorem~20.2 under the standard reading of the transcribed
conventions on $L^2([0,1], dx/x)$. \textbf{This is not a new finding.}
The author's existing audit catalog already documents these gaps. See
\texttt{DERIVATION\_ANALYSIS\_alpha\_NP.md} (2025-11-30) and
\texttt{MATHEMATICAL\_VALIDATION\_REPORT.md} (2025-11-30) at the
repository root, which (per the author's own assessment) catalog
$\alpha_{NP} = \varphi + 1/4$ as ``ASSERTED but NOT RIGOROUSLY DERIVED''
and identify several specific errors in the Chapter~21 fractal-monodromy
mechanism (the monodromy claim for $\mathrm{Li}_1$, the sine identity,
the eigenvalue ratio $(\sqrt{5}-1)/3$, and the closed form for
$\lambda_{NP}$). The 2026-04-26 verification pass independently
re-arrived at conclusions consistent with the author's own
\texttt{V01} catalog. In addition, the author's
\texttt{Evidence\_and\_Data\_for\_GitHub/fractal\_continuation\_derivation.py}
locates a numerical \emph{fix} for the principal-branch problem
($s \approx 0.182$, $m = -1$) that the manuscript text has not yet
adopted; aligning Chapter~21's text with that script is one of the
pending revision tasks.

What the verification setup observed (using the operator and inner
product as transcribed verbatim, with no convention adjustment):
\begin{itemize}
\item Numerical: $\langle \tilde{T}_3 x, x\rangle \approx -0.110 + 0.162\,i$
      under the verified setup (a self-adjoint operator should yield
      a real diagonal under the same setup);
      $\langle \tilde{T}_3 f, g\rangle - \langle f, \tilde{T}_3 g\rangle
       \approx 0.096 + 0.188\,i$ for $f=x$, $g=x^2$ (40-digit precision).
\item A reconciliation pass tested nine alternative readings of the
      manuscript notation (alternative weight, alternative inner-product
      conjugation slot, alternative phase placement, several alternative
      Hilbert-space structures, etc.); none of those readings rescued
      self-adjointness under the verified setup.
\end{itemize}

What this does NOT establish:
\begin{itemize}
\item It does not establish that the Principia Fractalis derivation is
      mathematically wrong. The verification setup uses the manuscript
      notation as currently transcribed; if the original V01 derivation
      uses any aspect of operator definition, inner-product convention,
      Hilbert-space structure, or phase placement that differs from
      the transcription, the verification result above does not apply.
\item Pabs's V01 catalog (the cataloged record of the original
      verification work) has not yet been located on disk; once it is,
      the verification setup will be reconciled against it. Resolution
      will fall in one of three buckets: (i) V01 conventions match the
      transcription and the open question is real, (ii) V01 conventions
      differ from the transcription in a way that restores self-adjointness
      and the open question dissolves, or (iii) a small refinement of
      the manuscript transcription brings it into alignment with V01.
\end{itemize}

\textbf{What this revision does for now}:

\begin{itemize}
\item The Lean axiom \texttt{T3\_self\_adjoint\_conj} (PF/TransferOperator.lean:221)
      is retained in source. Its docstring carries an open-verification-question
      note pending V01 reconciliation. Downstream proofs in
      \texttt{SpectralBijection.lean} continue to typecheck and remain
      inspectable.
\item The numerical evidence reported in Chapter~20 \S20.5--20.6
      (eigenvalue tabulation, 150-digit verification, the
      $\alpha^* = 5 \times 10^{-6}$ fit) is unchanged in this revision.
      If V01 reconciliation surfaces a refinement to the operator
      definition, the numerical evidence will be re-run against the
      reconciled operator.
\item Recovery options under investigation: (a) symmetrize via
      $(T+T^*)/2$, (b) augment with expanding-direction branches
      $y_k^{-1}(x) = bx - k$, (c) change measure to one for which
      the inverse-branch maps form a unitary representation
      (e.g.\ Gauss measure for the Gauss map), (d) downgrade
      Theorem~20.2 to a conjecture and restate the chapter's
      RH connection as a research program. Each of (a)--(c) is a
      structural redesign; the bibliography hint is the
      Lapidus/Mayer/Connes lineage.
\item This finding was caught \emph{by} the rev~2 rigor pass; in
      particular, by the very kind of independent verification the
      rev~2 frontmatter committed to. The other 7 axioms in the
      Lean canonical library are unaffected; build remains clean
      (\texttt{lake build}: 5486 jobs, zero errors, zero
      \texttt{sorry}).
\end{itemize}

\textit{End of post-rev-2 verification finding. The original rev~2
status text follows below for historical reference; readers should
treat the post-rev-2 finding above as the authoritative current
status of the T₃ axiom.}

\bigskip
\noindent\rule{\textwidth}{0.4pt}
\bigskip

The original rev~2 status (as of 2026-04-25, commit \texttt{88d5f37}):

\section*{Lean 4 layer (\texttt{PF\_Lean4\_Code/PF/})}

\begin{itemize}
\item \textbf{8 axioms remain} (down from 41 at the start of this revision cycle).
  \emph{Scope of the 8-axiom count:} this enumeration is the \emph{canonical} \texttt{PF\_Lean4\_Code/PF/} library only. The repository's top-level Lean files outside the canonical library (\texttt{PF\_Lean4\_Code/YM\_Equivalence.lean}, \texttt{PF\_Lean4\_Code/RH\_Equivalence.lean}, \texttt{PF\_Lean4\_Code/BSD\_Equivalence.lean}, and others) carry approximately 240 additional axioms representing exploratory framework-level statements not part of the canonical Lean~4 layer; those axioms are documented separately in \texttt{AXIOM\_AUDIT.md} at the repository root and are tracked toward elimination on a separate cycle. The 8-axiom claim of this manuscript refers exclusively to the canonical library at \texttt{PF\_Lean4\_Code/PF/}.
  Enumerated by name and location for referee transparency:
  \begin{itemize}
  \item \texttt{finite\_dim\_bochner} \hfill (\texttt{PF/CylindricalMeasures.lean:220}; CLASSIC, Ch~16)
  \item \texttt{bochner\_minlos\_existence} \hfill (\texttt{PF/BochnerMinlos.lean:81}; CLASSIC, Ch~23)
  \item \texttt{bochner\_minlos\_uniqueness} \hfill (\texttt{PF/BochnerMinlos.lean:93}; CLASSIC, Ch~23)
  \item \texttt{LogWeightedL2.inner} \hfill (\texttt{PF/TransferOperator.lean:70}; LOAD-BEARING PLACEHOLDER, Ch~20)
  \item \texttt{T3\_self\_adjoint\_conj} \hfill (\texttt{PF/TransferOperator.lean:221}; LOAD-BEARING PLACEHOLDER, Ch~20)
  \item \texttt{turingTimeComplexity} \hfill (\texttt{PF/TuringEncoding/Complexity.lean:57}; BOOK-CORE, Ch~21)
  \item \texttt{p\_eq\_np\_spectrum\_collapse} \hfill (\texttt{PF/TuringEncoding/Operators.lean:191}; BOOK-CORE, Ch~21)
  \item \texttt{operator\_collapse\_hypothesis} \hfill (\texttt{PF/P\_NP\_Complete\_Proof.lean:175}; BOOK-CORE, Ch~21)
  \end{itemize}
  Categories: \emph{CLASSIC} = standard analytic theorem not yet in mathlib (multi-month
  formalization effort); \emph{LOAD-BEARING PLACEHOLDER} = elimination requires further
  mathematical development before mechanization; \emph{BOOK-CORE} = conditional axiom
  representing one of the book's central conjectures (intentionally axiomatized).
\item \textbf{Zero \texttt{sorry} placeholders} anywhere in the library.
\item \texttt{lake build} completes all 5486 jobs cleanly.
\item During rev~2, \textbf{four latent unsoundness bugs} were identified and corrected.
  These were axioms whose stated content was provably inconsistent with definitions
  already present in the same file:
  \begin{itemize}
  \item \texttt{empty\_tape\_bound} (claimed $\log(2^s \cdot 3^h) \leq 0$, false at $s=1$)
  \item \texttt{characteristic\_cylindrical\_round\_trip} (contradicted the
        \texttt{normalized} field of \texttt{CharacteristicFunctional})
  \item \texttt{cylindrical\_measure\_fourier\_is\_characteristic} (same
        contradiction pattern)
  \item \texttt{nuclearity\_essential} (contradicted a trivial \texttt{NuclearSpace}
        witness construction that also lives in the codebase)
  \end{itemize}
\item \textbf{Classical results now machine-checked as theorems} (no longer axioms):
  $\sqrt{2}$ and $\varphi$ interval bounds at 8- and 10-digit precision;
  radix-economy monotonicity $\big(Q_{\text{decreasing}\geq 4}$, $Q_4 \geq Q_b\big)$;
  the strict maximum $\log b / b < 1/e$ for all $b > 1$, $b \neq e$ via the
  classical $\log t < t - 1$ lemma with substitution $t = b/e$; positive-definite
  Hermitian property $\big(C(-s) = \overline{C(s)}\big)$; and
  $\|C(s)\| \leq 1$ for PD-normalized functionals.
\item \textbf{Tight numerical bounds now machine-checked} (late-session work):
  $\pi/(10\sqrt{2}) > 0.222144146$ and $< 0.222144147$;
  $\pi/(10(\varphi + 1/4)) > 0.168176418$ and $< 0.168176419$;
  $|\pi/(10\sqrt{2}) - 0.2221441469| < 10^{-10}$;
  $|\pi/(10(\varphi + 1/4)) - 0.168176418230| < 10^{-9}$.
  These use \texttt{Real.pi\_gt\_d20} / \texttt{Real.pi\_lt\_d20} (20-digit $\pi$)
  and new supporting theorems $\sqrt{2},\sqrt{5},\varphi$ at 10-digit precision.
\item The \texttt{IsPositiveDefinite} definition itself was strengthened from a
  weak $.re \geq 0$ formulation to the standard real-and-non-negative formulation,
  unlocking the Hermitian and norm-bound proofs.
\end{itemize}

\section*{Yang--Mills cluster: honest placeholder disclosure}

The Yang--Mills-related theorems in \texttt{PF\_Lean4\_Code/PF/YangMillsMeasure.lean}
(and one in \texttt{GaussianModel.lean}) are now theorems with explicit
\textbf{``\(\triangle\) CURRENT PROOF CAVEAT''} docstrings. They are proven against
the current placeholder \texttt{CovarianceOperator.quadraticForm~:=~0} and therefore
establish the degenerate (zero-covariance) Gaussian, not the genuine non-trivial
Yang--Mills Gaussian. The scientific content that remains unproven is clearly
delimited; see Chapter~23 \S``Status of Analytical Construction'' for details.

\section*{Coq layer (\texttt{PF\_Coq/theories/})}

The Coq formalization contains \textbf{253 axioms/parameters} distributed across
32 files, covering broader scope than the Lean layer (Hodge, BSD, Navier--Stokes,
and clinical validation are Coq-only). The axiom eliminations achieved in Lean
during rev~2 have \textbf{not yet been ported to Coq}; this is flagged as future
work in \texttt{PARITY\_REPORT.md} at the repository root.

\section*{Lean4Lean layer (formerly \texttt{PF\_L4L/}, now quarantined)}

A skeletal Lean4Lean meta-verification layer was begun in earlier
revisions, but it was never made to build (the package required a
canonical-source directory that does not exist in the current repo
layout, and the imports it expects do not match the current Lean
file structure). To avoid misleading a referee with a non-buildable
``third formalization layer,'' the directory has been moved to
\texttt{experimental/PF\_L4L\_future/} and is \textbf{not} part of
the rev~2 claim. Restoring it to a working state would require
either restructuring the Lean code organization or rewriting the
L4L imports against the current layout; both are tracked as future
work in \texttt{RESEARCH\_ROADMAP.md}.

\section*{Reviewer guidance}

For each chapter where a theorem has been machine-checked during rev~2, a
``Formal verification (rev 2)'' note has been added to the relevant proof
environment. Currently such notes appear in:
\begin{itemize}
\item Chapter~3 ``Complexity Separation'' theorem --- four $\lambda_0$
      closed-form theorems (\texttt{lambda\_P\_lower/upper\_certified},
      \texttt{lambda\_NP\_lower/upper\_certified}, \texttt{lambda\_0\_P\_precise},
      \texttt{lambda\_0\_NP\_precise}) with supporting 10-digit
      $\sqrt{2},\sqrt{5},\varphi$ bounds.
\item Chapter~7 ``Ternary Optimality'' proof --- three theorems now Lean~4
      machine-checked: \texttt{Q\_decreasing\_from\_4}, \texttt{Q\_4\_ge\_Q\_larger},
      and the strict global maximum \texttt{radix\_economy\_max\_at\_exp1}.
\item Chapter~16 ``Nuclear C*-Algebras'' critical-line argument --- a
      ``Formalization status'' remark discloses that the previous rev's
      \texttt{nuclearity\_essential} axiom was deleted as latently unsound,
      and identifies what real \texttt{NuclearSpace.nuclear\_property}
      would be needed to recover a machine-checked argument.
\item Chapter~20 ``Self-Adjointness Theorem'' --- expanded status note: the
      axiom \texttt{T3\_self\_adjoint\_conj} stays (LOAD-BEARING PLACEHOLDER)
      but the two classical supporting theorems
      \texttt{pos\_def\_hermitian} and \texttt{pos\_def\_normalized\_bounded}
      are now machine-checked via the strengthened \texttt{IsPositiveDefinite}
      definition; also cites \texttt{PF/LogWeightedIntegral.lean}
      (foundation for the future elimination).
\item Chapter~21 chapter-opening ``Status (rev 2)'' paragraph, ``Closed Form
      Candidate'' observation, and ``Main Conjecture'' remark --- the main
      conjecture is now labeled a \emph{conditional theorem modulo three
      BOOK-CORE axioms}, with five machine-checked downstream theorems
      enumerated.
\item Chapter~23 ``Toroidal curvature shells'' paragraph --- citation of the
      \texttt{CurvatureShell.alpha\_natural} and
      \texttt{TimelessFieldTorus.embedding\_mono} promotions.
\item Chapter~23 ``Status of Analytical Construction'' remark --- full
      enumeration of the 8 YM-cluster theorems proven against the
      zero-covariance placeholder, each with an explicit
      ``$\triangle$ CURRENT PROOF CAVEAT'' docstring in the Lean source.
\item Chapter~22 ``Connection to Consciousness'' (preceding remark) ---
      new ``Formalization status, rev 2'' note discloses that
      Navier--Stokes is Coq-only with $\sim$23 framework axioms, and
      that the existing \texttt{Theorem PF\_NS\_Solution} in
      \texttt{PF\_Coq/theories/Contracts/NavierStokes.v} is a
      one-line application of two of those local axioms.
\item Chapter~24 ``Connection to Consciousness'' (preceding remark) ---
      new ``Formalization status, rev 2'' note discloses that
      Birch--Swinnerton-Dyer is Coq-only with $\sim$20 framework
      axioms encoding the spectral-operator $T_E$ structure and the
      rank-equals-multiplicity identity (axiomatized, not theorem).
\item Chapter~25 ``Connection to Consciousness'' (preceding remark) ---
      new ``Formalization status, rev 2'' note discloses that the
      Hodge Conjecture is Coq-only with $\sim$19 framework axioms,
      and that the existing \texttt{Theorem PF\_Hodge\_Conjecture}
      in \texttt{PF\_Coq/theories/Contracts/Hodge.v} is a one-line
      application of two of those local axioms.
\end{itemize}

\section*{Coq honesty pass (Contracts layer)}

In rev~2 a referee-disclosure pass was added to the Coq
\texttt{Contracts/} layer. Each ``Theorem'' that establishes a
Millennium-Problem proposition (\texttt{PF\_NS\_Solution} in
\texttt{NavierStokes.v}, \texttt{PF\_Hodge\_Conjecture} in
\texttt{Hodge.v}) and each ``Theorem'' that wraps an underlying
\texttt{Axiom} (\texttt{RH\_T3\_self\_adjoint},
\texttt{RH\_T3\_compact} in \texttt{RH.v}) now carries a prominent
in-source comment block stating: (i) the theorem is conditional, (ii)
which local axioms its proof depends on, (iii) the
\texttt{Print Assumptions} command that recovers the full dependency
list, and (iv) the cross-reference to the corresponding manuscript
chapter. No proofs were modified; the Coq build still completes
cleanly.
The corresponding machine-checked artefacts are in the Lean 4 source at
\texttt{PF\_Lean4\_Code/PF/} on the repository master branch.

The canonical source of truth for all claims above is the GitHub repository
\texttt{FractalDevTeam/Principia-Fractalis}, at or after commit \texttt{88d5f37}.
Foundational infrastructure for the eventual elimination of
\texttt{LogWeightedL2.inner} and \texttt{T3\_self\_adjoint\_conj} has now been laid
in \texttt{PF/LogWeightedIntegral.lean}: the log-weighted measure
$\mu := \mathrm{volume.withDensity}(1/x)$ is defined via mathlib's
\texttt{Measure.withDensity}, the supporting \texttt{SigmaFinite} instance is
machine-checked, and the concrete Hilbert space
\texttt{LogWeightedL2\_concrete := MeasureTheory.Lp $\mathbb{C}$ 2 logWeightedMeasure}
is in scope and inherits its inner product from mathlib automatically. The remaining
work to retire the two axioms is (a) refactoring \texttt{PF/TransferOperator.lean}
to use this concrete type in place of the current placeholder \texttt{structure},
and (b) supplying the kernel-symmetry calculation that the pen-and-paper proof of
Theorem~20.2 currently leaves implicit.
A detailed audit table (topic-by-topic axiom counts in Lean~4 vs Coq)
is maintained at \texttt{PARITY\_REPORT.md} in the repository root.

\section*{Author bibliography placeholders}\label{sec:cohen2025-bib-status}
The bibliography contains approximately 18 entries beginning with
the prefix \texttt{cohen2025*} (e.g., \texttt{cohen2025navierstokes},
\texttt{cohen2025pvsnp}, \texttt{cohen2025universal}) marked
\texttt{howpublished = \{Unpublished manuscript\}}. These are placeholder
citations for the author's preprint series accompanying this
manuscript. As individual papers from the series are uploaded to
arXiv, the corresponding bibliography entries will be replaced with
arXiv identifiers and DOIs in subsequent edits to
\texttt{Principia\_Fractalis\_master\_folder\_rev2/bibliography.bib}.
Until those uploads occur, the placeholder citations honestly mark
the status as unpublished, matching the practice of academic monographs
that cite forthcoming companion material.

